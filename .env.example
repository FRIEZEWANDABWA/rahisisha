# Environment Variables Template
# Add these EXACT values in Netlify Dashboard → Site Settings → Environment Variables
# NEVER commit the actual .env file to GitHub!

# Your n8n webhook URL (KEEP SECRET - only in Netlify dashboard)
N8N_WEBHOOK_URL=https://n8n.rahisisha.tech/webhook-test/rahisisha_chatbot

# Optional: Add API key for extra security
N8N_API_KEY=your-secret-key-here

# Rate limiting (requests per minute per IP)
RATE_LIMIT_REQUESTS=20
RATE_LIMIT_WINDOW=60